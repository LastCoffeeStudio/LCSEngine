#version 330 core

in vec2 UV;
in vec3 col;

out vec4 color;

uniform sampler2D text;
uniform bool useText;

void main()
{
	if (useText)
	{
		color = vec4(texture(text,UV).rgb * col, texture(text,UV).a);
	}
	else
	{
		color = vec4(col, 1.0f);
	}
}

